close all
clear all

iters = [
% Build the matrix
N=20;
[j,i] = meshgrid(1:N, 1:N);
A  = 1 ./ (i+j-1);
b  = ones(N,1);
x0 = zeros(N,1);
vec_eps = 1e-6;

[x_opt, num_of_iters] = conj_grad (x0, A, b, vec_eps);

[x_opt] = conj_grad (x0, A, b, vec_eps);
A*x_opt - b
vec_norm(A*x_opt - b)
